# Aura Studio

Mobile-first portfolio site for Aura Studio: AI UGC videos and product visuals.

- `index.html` — the whole site (no build step)
- `videos/` — 9:16 MP4s (720x1280, H.264) and poster images

Before going live, replace `YOUR_HANDLE` and `YOUR_EMAIL` in `index.html`.
